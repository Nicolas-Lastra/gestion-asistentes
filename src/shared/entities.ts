export interface Student {
  id: string;
  name: string;
  surname: string;
  dni: string;
  email: string;
}

export interface Course {
  name: string;
  code: string;
  credits: number;
  id: string;
}